<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.luca.sabatini.appello">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".Appello"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".StatisticsActivity"></activity>
        <activity android:name=".attendancesession.AttendanceSession" />
        <activity android:name=".student.ConfermaPresenza" />
        <activity android:name=".student.RicercaAppelli" />
        <activity android:name=".student.ProfiloUtente" />
        <activity android:name=".student.CameraActivity" />
        <!--
                PER MODIFICARE L'ACTIVITY DI INIZIO DEVI ANDARE NELLA CLASSE SplashScrenn
                E MODIFICARE IL SECONDO PARAMETRO DELL'INTENT
        -->
        <activity
            android:name=".AppelloOrStatistica"
            android:parentActivityName=".list.ListaCorsi" />
        <activity android:name=".login.ForgotPassword" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".list.ListaCorsi" />
        <activity android:name=".VerifyStudent" />
        <activity android:name=".MainActivity" />

        <service
            android:name=".attendancesession.FirebaseService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>